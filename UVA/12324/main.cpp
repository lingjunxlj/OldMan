#include <iostream>
#include <cstdio>
#include <cstring>
#include <algorithm>
#include <cmath>
using namespace std;
int t[i];
int d[i];
int main()
{
    int n;
    while(scanf("%d", &n), n)
    {
        for (int i = 0; i < n; i++)
            scanf("%d", &t, &d);

    }
}
