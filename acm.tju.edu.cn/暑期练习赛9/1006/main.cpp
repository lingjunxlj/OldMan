#include <iostream>
#include <cstdio>
#include <cstring>
#include <algorithm>
using namespace std;
string a, b, c;
char d[200002];
int main()
{
    while (cin>>a>>b)
    {
        c = a + b;
        int len = a.size();
        for (int i = 0; i < len; i++)
        {
            if ()
        }
    }
}
