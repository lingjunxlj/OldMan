#include <cstdio>
#include <cstring>
int dp[500][500];
int a[300],b[300];
int main()
{
    int t;
    scanf("%d",&t);
    while (t--)
    {
        int Case = 0;
        int n,x,y;
        scanf("%d %d %d",&n,&x,&y);
        int i,j;
        for (i=1; i<=n; i++)
        scanf("%d %d",a[i],b[i]);

        memset(dp,0,sizeof(dp));





    }
}
